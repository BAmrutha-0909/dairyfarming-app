<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dairyfarming">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>


    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <application
        android:allowBackup="true"
        android:label="DairyFarming"
        android:icon="@mipmap/app_logo"
        android:roundIcon="@mipmap/app_logo"
        android:theme="@style/Theme.DairyFarming">


    <!-- Splash Screen -->
    <activity
        android:name=".ui.SplashActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
    </activity>

    <!-- Auth Activities -->
    <activity android:name=".ui.LoginActivity" android:exported="true"/>
    <activity android:name=".ui.SignupActivity" android:exported="true"/>

    <!-- Main Activities -->
    <activity android:name=".ui.DashboardActivity" android:exported="true"/>
    <activity android:name=".ui.ProfileActivity" android:exported="true"/>
        <activity android:name=".ui.SummaryActivity"/>
        <activity android:name=".ui.SalesProfitActivity"/>
        <activity android:name=".ui.AccountDetailsActivity"/>
        <activity android:name=".ui.SettingsActivity"/>

        <receiver android:name=".ui.fragments.NotificationReceiver"/>




        <!-- Other Activities -->
    <activity android:name=".ui.NotificationActivity" android:exported="true"/>
    <activity android:name=".ui.ChangePasswordActivity" android:exported="true"/>
    <activity android:name=".ui.ExportDataActivity" android:exported="true"/>
    <activity android:name=".ui.AboutAppActivity" android:exported="true"/>

</application>
    </manifest>
